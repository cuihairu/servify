{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "title": "AI Request Volume",
      "type": "timeseries",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "targets": [
        {
          "expr": "sum(rate(ai_requests_total[5m])) by (provider, strategy)",
          "legendFormat": "{{provider}}/{{outcome}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "ops/s"
        }
      }
    },
    {
      "title": "AI Request Latency",
      "type": "timeseries",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(ai_request_duration_seconds_bucket[5m])) by (le, provider))",
          "legendFormat": "{{provider}} p50"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(ai_request_duration_seconds_bucket[5m])) by (le, provider))",
          "legendFormat": "{{provider}} p95"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        }
      }
    },
    {
      "title": "LLM Token Consumption",
      "type": "timeseries",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 8
      },
      "targets": [
        {
          "expr": "sum(rate(ai_llm_tokens_total[1h])) by (provider, token_type)",
          "legendFormat": "{{provider}}/{{token_type}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "tokens/h"
        }
      }
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 8
      },
      "id": 124,
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "single"
        }
      },
      "targets": [
        {
          "expr": "sum(rate(ai_requests_total[5m])) by (strategy)",
          "legendFormat": "__auto",
          "refId": "A"
        }
      ],
      "title": "AI Strategy Breakdown",
      "type": "timeseries",
      "description": "AI 答复策略分布（primary=知识源命中，fallback=降级，transfer=转人工）"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 16
      },
      "id": 136,
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom"
        },
        "tooltip": {
          "mode": "single"
        }
      },
      "targets": [
        {
          "expr": "sum(rate(ai_requests_total{strategy=\"fallback\"}[10m])) / sum(rate(ai_requests_total{strategy=~\"primary|fallback\"}[10m]))",
          "legendFormat": "__auto",
          "refId": "A"
        }
      ],
      "title": "AI Fallback Ratio",
      "type": "timeseries",
      "description": "降级答复占比（不含转人工）；持续高于 50% 触发 AIFallbackRatioHigh"
    }
  ],
  "schemaVersion": 39,
  "tags": [
    "servify",
    "business"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-1h",
    "to": "now"
  },
  "title": "Servify - Business Metrics",
  "uid": "servify-business"
}
